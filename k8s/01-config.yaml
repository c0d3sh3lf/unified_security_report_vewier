apiVersion: v1
kind: ConfigMap
metadata:
  name: security-reports-config
  namespace: security-reports
data:
  API_PORT: '4000'
  JWT_EXPIRES_IN: '8h'
  CORS_ORIGIN: 'https://ci-reports.invadersam.cloud'
  DEFAULT_ADMIN_NAME: 'Platform Administrator'
  DEFAULT_ADMIN_EMAIL: 'admin@security.local'
